import React from 'react';
import {starsWarsInfo} from "../utils/constants";

const StarWars = () =>
{
    return (
        <div>
            <p>{starsWarsInfo}</p>
        </div>
    );
};

export default StarWars;
